# OpenAI is used by the imagegen Cloud Function (gpt-image-1) AND by the app
# for moderation (free) + child-safe text generation (gpt-4o-mini).
OPENAI_API_KEY=sk-your-openai-api-key-here

IMAGE_GEN_API_URL=https://generatestoryimages-lmpxea5clq-uc.a.run.app
